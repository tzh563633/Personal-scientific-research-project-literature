# Infrastructure

Docker Compose is kept at the repository root so it can be started with one command.
This directory is reserved for deployment-specific overrides, health checks, and
future reverse-proxy configuration without mixing those files into application code.
